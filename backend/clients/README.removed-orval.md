This folder previously contained Orval-generated API clients (from `orval.config.js` and `swagger-spec.json`).

As of this change, Orval is no longer used in this project, and the generated client code has been removed at the user's request. If you need a typed client in the future, consider generating it from the live OpenAPI spec using your preferred tooling or maintaining a hand-written client.
