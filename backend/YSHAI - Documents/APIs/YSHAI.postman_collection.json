{
  "info": {
    "name": "YSHAI API",
    "description": "Full Postman Collection for YSHAI backend API (Auth, Users, Teams, Posts, Media, AI, Moderation, Analytics, Billing, Notifications, Webhooks, Meta, Admin).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Auth",
      "item": [
        {
          "name": "Register",
          "request": { "method": "POST", "url": "{{baseUrl}}/auth/register" }
        },
        {
          "name": "Login",
          "request": { "method": "POST", "url": "{{baseUrl}}/auth/login" }
        },
        {
          "name": "Logout",
          "request": { "method": "POST", "url": "{{baseUrl}}/auth/logout" }
        },
        {
          "name": "Refresh Token",
          "request": { "method": "POST", "url": "{{baseUrl}}/auth/refresh" }
        },
        {
          "name": "Forgot Password",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/auth/forgot-password"
          }
        },
        {
          "name": "Reset Password",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/auth/reset-password"
          }
        }
      ]
    },
    {
      "name": "Users",
      "item": [
        {
          "name": "Get Current User",
          "request": { "method": "GET", "url": "{{baseUrl}}/users/me" }
        },
        {
          "name": "Update Profile",
          "request": { "method": "PUT", "url": "{{baseUrl}}/users/me" }
        },
        {
          "name": "Set Language & Dialect",
          "request": {
            "method": "PUT",
            "url": "{{baseUrl}}/users/me/preferences"
          }
        }
      ]
    },
    {
      "name": "Teams",
      "item": [
        {
          "name": "Create Team",
          "request": { "method": "POST", "url": "{{baseUrl}}/teams" }
        },
        {
          "name": "Invite Member",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/teams/{{teamId}}/invite"
          }
        },
        {
          "name": "Update Member Role",
          "request": {
            "method": "PUT",
            "url": "{{baseUrl}}/teams/{{teamId}}/members/{{memberId}}"
          }
        },
        {
          "name": "Remove Member",
          "request": {
            "method": "DELETE",
            "url": "{{baseUrl}}/teams/{{teamId}}/members/{{memberId}}"
          }
        },
        {
          "name": "Approve Post",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/teams/{{teamId}}/posts/{{postId}}/approve"
          }
        },
        {
          "name": "Reject Post",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/teams/{{teamId}}/posts/{{postId}}/reject"
          }
        },
        {
          "name": "Audit Logs",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/teams/{{teamId}}/audit-logs"
          }
        }
      ]
    },
    {
      "name": "Accounts",
      "item": [
        {
          "name": "Link Account",
          "request": { "method": "POST", "url": "{{baseUrl}}/accounts" }
        },
        {
          "name": "Unlink Account",
          "request": {
            "method": "DELETE",
            "url": "{{baseUrl}}/accounts/{{accountId}}"
          }
        }
      ]
    },
    {
      "name": "Posts",
      "item": [
        {
          "name": "Create Post",
          "request": { "method": "POST", "url": "{{baseUrl}}/posts" }
        },
        {
          "name": "Update Post",
          "request": { "method": "PUT", "url": "{{baseUrl}}/posts/{{postId}}" }
        },
        {
          "name": "Delete Post",
          "request": {
            "method": "DELETE",
            "url": "{{baseUrl}}/posts/{{postId}}"
          }
        },
        {
          "name": "Bulk Upload Posts",
          "request": { "method": "POST", "url": "{{baseUrl}}/posts/bulk" }
        },
        {
          "name": "Recurring Post",
          "request": { "method": "POST", "url": "{{baseUrl}}/posts/recurring" }
        },
        {
          "name": "Draft Post",
          "request": { "method": "POST", "url": "{{baseUrl}}/posts/draft" }
        },
        {
          "name": "Reschedule Post",
          "request": {
            "method": "PUT",
            "url": "{{baseUrl}}/posts/{{postId}}/reschedule"
          }
        },
        {
          "name": "Publish Now",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/posts/{{postId}}/publish"
          }
        },
        {
          "name": "Post Status",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/posts/{{postId}}/status"
          }
        }
      ]
    },
    {
      "name": "Media",
      "item": [
        {
          "name": "Upload Media",
          "request": { "method": "POST", "url": "{{baseUrl}}/media" }
        },
        {
          "name": "List Media",
          "request": { "method": "GET", "url": "{{baseUrl}}/media" }
        },
        {
          "name": "Delete Media",
          "request": {
            "method": "DELETE",
            "url": "{{baseUrl}}/media/{{mediaId}}"
          }
        }
      ]
    },
    {
      "name": "AI",
      "item": [
        {
          "name": "Generate Caption",
          "request": { "method": "POST", "url": "{{baseUrl}}/ai/caption" }
        },
        {
          "name": "Generate Hashtags",
          "request": { "method": "POST", "url": "{{baseUrl}}/ai/hashtags" }
        },
        {
          "name": "Generate Alt Text",
          "request": { "method": "POST", "url": "{{baseUrl}}/ai/alt-text" }
        }
      ]
    },
    {
      "name": "Moderation",
      "item": [
        {
          "name": "Moderate Text",
          "request": { "method": "POST", "url": "{{baseUrl}}/moderation/text" }
        },
        {
          "name": "Moderate Image",
          "request": { "method": "POST", "url": "{{baseUrl}}/moderation/image" }
        },
        {
          "name": "Moderate Video",
          "request": { "method": "POST", "url": "{{baseUrl}}/moderation/video" }
        },
        {
          "name": "Moderation Reports",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/moderation/reports"
          }
        }
      ]
    },
    {
      "name": "Analytics",
      "item": [
        {
          "name": "Post Insights",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/analytics/posts/{{postId}}"
          }
        },
        {
          "name": "Account Insights",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/analytics/accounts/{{accountId}}"
          }
        },
        {
          "name": "Campaign Insights",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/analytics/campaigns/{{campaignId}}"
          }
        },
        {
          "name": "Export Report (CSV)",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/analytics/export/csv"
          }
        },
        {
          "name": "Export Report (PDF)",
          "request": {
            "method": "GET",
            "url": "{{baseUrl}}/analytics/export/pdf"
          }
        }
      ]
    },
    {
      "name": "Billing",
      "item": [
        {
          "name": "List Plans",
          "request": { "method": "GET", "url": "{{baseUrl}}/billing/plans" }
        },
        {
          "name": "Create Subscription",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/billing/subscribe"
          }
        },
        {
          "name": "Cancel Subscription",
          "request": { "method": "DELETE", "url": "{{baseUrl}}/billing/cancel" }
        },
        {
          "name": "Get Invoices",
          "request": { "method": "GET", "url": "{{baseUrl}}/billing/invoices" }
        },
        {
          "name": "Payment Callback (HyperPay/PayTabs)",
          "request": { "method": "POST", "url": "{{baseUrl}}/billing/callback" }
        }
      ]
    },
    {
      "name": "Notifications",
      "item": [
        {
          "name": "List Notifications",
          "request": { "method": "GET", "url": "{{baseUrl}}/notifications" }
        },
        {
          "name": "Mark Notification as Read",
          "request": {
            "method": "PUT",
            "url": "{{baseUrl}}/notifications/{{notificationId}}/read"
          }
        }
      ]
    },
    {
      "name": "Webhooks",
      "item": [
        {
          "name": "Register Webhook",
          "request": { "method": "POST", "url": "{{baseUrl}}/webhooks" }
        },
        {
          "name": "List Webhooks",
          "request": { "method": "GET", "url": "{{baseUrl}}/webhooks" }
        },
        {
          "name": "Delete Webhook",
          "request": {
            "method": "DELETE",
            "url": "{{baseUrl}}/webhooks/{{webhookId}}"
          }
        }
      ]
    },
    {
      "name": "Meta",
      "item": [
        {
          "name": "Health Check",
          "request": { "method": "GET", "url": "{{baseUrl}}/health" }
        },
        {
          "name": "List Timezones",
          "request": { "method": "GET", "url": "{{baseUrl}}/meta/timezones" }
        },
        {
          "name": "List Locales",
          "request": { "method": "GET", "url": "{{baseUrl}}/meta/locales" }
        }
      ]
    },
    {
      "name": "Admin",
      "item": [
        {
          "name": "List Users",
          "request": { "method": "GET", "url": "{{baseUrl}}/admin/users" }
        },
        {
          "name": "Ban User",
          "request": {
            "method": "POST",
            "url": "{{baseUrl}}/admin/users/{{userId}}/ban"
          }
        },
        {
          "name": "Assign Role",
          "request": {
            "method": "PUT",
            "url": "{{baseUrl}}/admin/users/{{userId}}/role"
          }
        },
        {
          "name": "Audit Logs",
          "request": { "method": "GET", "url": "{{baseUrl}}/admin/audit-logs" }
        }
      ]
    }
  ],
  "variable": [{ "key": "baseUrl", "value": "http://localhost:5000/api/v1" }]
}
